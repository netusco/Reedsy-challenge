import app from '../../index'
import style from '../../config/tests' // needed for test suites style

// Runs before any test suite
// NOTE: this file should run before any other test so it should be saved with a prioritary alphabet order
before(function() {
})
